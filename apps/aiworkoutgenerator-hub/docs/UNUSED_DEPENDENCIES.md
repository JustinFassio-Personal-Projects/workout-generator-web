# Unused Dependencies Report

> **Last updated:** 2025-12-29
>
> This report is generated by running `npx depcheck --json`. To regenerate this report when dependencies change, run the command and update the date above.

## Unused Dependencies (can be removed)

### Regular Dependencies

- `@hookform/resolvers` - Not currently used (likely planned for future form validation)
- `@tailwindcss/typography` - Not currently used
- `date-fns` - Not currently used (likely planned for date formatting)
- `tailwindcss-animate` - Not currently used
- `zod` - Not currently used (likely planned for schema validation)

### Dev Dependencies

- `@tailwindcss/postcss` - Not currently used (may be needed for Tailwind v4)
- `@vitest/coverage-v8` - Not currently used (coverage testing not configured)
- `autoprefixer` - Not currently used (may be needed for PostCSS)
- `hono` - **REMOVE** - API framework not compatible with static export
- `lint-staged` - Not currently used (Husky pre-commit hooks not fully configured)
- `tailwindcss` - Not currently used (may be needed for Tailwind v4)

## Recommendations

### High Priority - Remove Now

1. **`hono`** - API framework incompatible with static export configuration

### Medium Priority - Consider Removing

2. `@vitest/coverage-v8` - If not planning to use coverage reports
3. `lint-staged` - If not using pre-commit hooks

### Low Priority - Keep for Future Use

4. `@hookform/resolvers` - Likely needed when adding Zod validation to forms
5. `zod` - Likely needed for schema validation (good practice)
6. `date-fns` - Likely needed for date formatting in calendar/scheduling features
7. `tailwindcss-animate` - May be used in custom animations
8. `@tailwindcss/typography` - May be needed for blog/content pages

### Uncertain - Verify Before Removing

9. `@tailwindcss/postcss` - May be required for Tailwind CSS v4
10. `autoprefixer` - May be required for PostCSS pipeline
11. `tailwindcss` - May be required as peer dependency

## Action Items

### Immediate

```bash
npm uninstall hono
```

### Optional (if not planning to use)

```bash
npm uninstall @vitest/coverage-v8 lint-staged
```

### Keep for Now

The following are likely planned for future features and should be kept:

- `@hookform/resolvers`
- `zod`
- `date-fns`
- `tailwindcss-animate`
- `@tailwindcss/typography`

## Notes

- Some dependencies may be indirect/peer dependencies and shouldn't be removed
- Verify build still works after removing any dependencies
- Some "unused" dependencies may be required by the build toolchain even if not directly imported
