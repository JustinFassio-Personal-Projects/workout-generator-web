{
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "dev:main": "turbo run dev --filter=main-web",
    "dev:admin": "turbo run dev --filter=admin-dash",
    "dev:squat": "turbo run dev --filter=squat-test"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
