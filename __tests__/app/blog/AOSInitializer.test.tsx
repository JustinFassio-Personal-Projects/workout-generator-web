import { describe, it, expect } from 'vitest'
import { AOSInitializer } from '@/app/blog/AOSInitializer'

describe('AOSInitializer', () => {
  it('should be defined', () => {
    expect(AOSInitializer).toBeDefined()
  })
})
